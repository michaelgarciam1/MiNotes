<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MiNotes" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Configuración de color de la barra de estado -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item> <!-- Coloca iconos oscuros en la barra de estado para fondos claros -->
        <!-- Color de fondo de la actividad -->
        <item name="android:colorBackground">@color/white</item>
    </style>
</resources>